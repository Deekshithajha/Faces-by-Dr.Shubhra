import type { ProcedureCategory } from "@/types";

/** Category buckets are not used for treatment navigation; see `treatmentSections.ts`. */
export const procedureCategories: ProcedureCategory[] = [];
